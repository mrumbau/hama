# Root .env.example — copy to .env (gitignored) and fill in.
# This file is loaded by docker-compose for cross-service shared values.

# ── Service ports ────────────────────────────────────────────────────────────
SERVER_PORT=5000
CLIENT_PORT=5173
ML_PORT=8001
REDIS_PORT=6379
