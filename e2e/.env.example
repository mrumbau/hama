# e2e/.env — copied to e2e/.env (gitignored). Loaded by playwright.config.ts
# via the root dotenv import.
#
# Operator credentials for the storage-state setup spec. The user must
# already exist in the live Supabase project (auth.users) — Tag 5
# acknowledges hawramimohammed@gmail.com as the seed operator.

E2E_TEST_USER_EMAIL=
E2E_TEST_USER_PASSWORD=

# Override the base URL when the dev client is on a non-default port.
# E2E_BASE_URL=http://localhost:5173
