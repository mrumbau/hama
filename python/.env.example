# python/.env.example — copy to python/.env (gitignored).
# ML service is internal-only; it does not hold external API keys.

ML_HOST=0.0.0.0
ML_PORT=8001
ML_WORKERS=2

REDIS_URL=redis://127.0.0.1:6379

# InsightFace model pack. buffalo_l = ArcFace 512-D + RetinaFace.
INSIGHTFACE_MODEL_PACK=buffalo_l
INSIGHTFACE_DET_SIZE=640

# Quality gate thresholds. Reasons returned to the operator UI:
#   face_too_small  · QUALITY_MIN_FACE_PX        — short edge of the face bbox
#   too_blurry      · QUALITY_MIN_BLUR_VAR       — Laplacian variance on the central-60% face crop (HIGHER = sharper)
#   pose_extreme    · QUALITY_MAX_POSE_YAW_DEG   — |yaw| derived from 5-point landmarks
#
# QUALITY_MIN_BLUR_VAR is a heuristic. 40 covers modern smartphone front-cams
# with sensor smoothing (typical Laplacian variance 60-75). The original 80
# was DSLR-calibrated and over-rejected legitimate iPhone selfies. Tag 13
# substitutes with an empirical threshold from a 30-selfie sample —
# see EVALUATION.md backlog "Quality-gate calibration" + DECISIONS.md D-015.
QUALITY_MIN_FACE_PX=112
QUALITY_MIN_BLUR_VAR=40
QUALITY_MAX_POSE_YAW_DEG=45

# Detector confidence floor (RetinaFace → keep face if score >= this)
DETECTOR_MIN_SCORE=0.5
